[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "tools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest",
    "playwright",
    "pytest-playwright",
]
